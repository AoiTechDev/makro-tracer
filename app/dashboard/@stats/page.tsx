import { Card } from "@/components/ui/card";
import React from "react";

const page = () => {
  return (
    <Card className="min-h-[400px] flex-1 bg-red-200 lg:flex justify-center items-center hidden">
      Existing meals(comming soon)
    </Card>
  );
};

export default page;
